<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1><%= genre.name %></h1>
    <ul>
        <% for(let i=0; i<genre.movies.length; i++) { %>
            <li><%= genre.movies[i].title %> </li> 
        <% } %> 
    </ul>
</body>
</html>